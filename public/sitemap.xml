<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <!-- Homepage -->
    <url>
        <loc>{{ url('/') }}</loc>
        <lastmod>{{ now()->format('Y-m-d') }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>
    
    <!-- Domain Browse Page -->
    <url>
        <loc>{{ route('domains.public.index') }}</loc>
        <lastmod>{{ now()->format('Y-m-d') }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>0.9</priority>
    </url>
    
    <!-- Auth Pages -->
    <url>
        <loc>{{ route('login') }}</loc>
        <lastmod>{{ now()->format('Y-m-d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>
    
    <url>
        <loc>{{ route('register') }}</loc>
        <lastmod>{{ now()->format('Y-m-d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>
    
    <!-- Individual Domain Pages (if you have published domains) -->
    @if(isset($publishedDomains) && $publishedDomains && $publishedDomains->count() > 0)
        @foreach($publishedDomains as $domain)
            @if($domain && $domain->user)
                <url>
                    <loc>{{ route('domains.show', $domain) }}</loc>
                    <lastmod>{{ $domain->updated_at->format('Y-m-d') }}</lastmod>
                    <changefreq>weekly</changefreq>
                    <priority>0.8</priority>
                </url>
            @endif
        @endforeach
    @endif
</urlset>
